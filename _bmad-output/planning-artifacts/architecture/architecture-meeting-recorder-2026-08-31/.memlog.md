---
scope: Minutes — macOS menu bar meeting recorder, whole system
purpose: build-substrate
altitude: feature (keeps epics)
updated: 2026-09-04T13:59
---

- (event by coach) Headless create. Inputs by reference: prd.md (48 FRs, build-order tiers in §6.3), DESIGN.md + EXPERIENCE.md spines, brief addendum (verified platform research). Inheriting settled decisions silently rather than re-deciding.
- (version by coach) VERIFIED from the real manifest at the tag (curl of Package.swift @ v1.1.0), not from README or training data: argmax-oss-swift v1.1.0 exposes library products ArgmaxOSS, WhisperKit, TTSKit, SpeakerKit; platforms macOS 13+/iOS 16+; SpeakerKit target depends on WhisperKit + ArgmaxCore; only non-server external dep is swift-argument-parser (CLI target only).
- (constraint by coach) argmax-oss-swift declares swiftLanguageVersions: [.v5] and enables StrictConcurrency experimentally. Consequence: an app target in Swift 6 language mode risks Sendable/isolation compile errors crossing into it. Decision recorded as AD — app target uses Swift 5 language mode to keep a same-day build from drowning in concurrency diagnostics.
- (decision by coach) Link WhisperKit and SpeakerKit individually, NOT the ArgmaxOSS umbrella — the umbrella drags TTSKit in for a product that does no speech synthesis.
- (event by coach) SPIKE 1 (build/sign/bundle) PASSED: SPM executable + SwiftUI MenuBarExtra builds in Swift 5 language mode under swift-tools 6.0; hand-assembled .app bundle with Info.plist (LSUIElement, NSMicrophoneUsageDescription, NSAudioCaptureUsageDescription) ad-hoc signed with --options runtime + entitlements verifies as adhoc+runtime with identifier set. spctl rejects, which is expected and irrelevant for a locally built app. No Xcode project needed anywhere.
- (decision by coach) AD: build system is SPM + a shell script that assembles and ad-hoc signs the .app bundle. No .xcodeproj. Rationale: fully scriptable and reproducible without the Xcode GUI, and gives exact control of Info.plist and the signature, which TCC depends on.
- (event by coach) SPIKE 2 (CoreAudio process tap) PASSED after 4 iterations: captured real system audio — frames=117600, nonZeroCallbacks=128, peak=0.359 over a 5s window — with clean teardown and no leftover aggregate devices. Verified working while the default output was Bluetooth (AirPods), not just built-in speakers.
- (constraint by coach) CRITICAL, CONTRADICTS PUBLISHED GUIDANCE: AudioDeviceCreateIOProcIDWithBlock(_:_:queue:block:) DEADLOCKS INDEFINITELY on a tap-backed aggregate device — reproduced on both the main thread and a background queue, no crash, no TCC log entry, process simply hangs inside the call. AudioDeviceCreateIOProcID with a classic C function pointer works first time. The web article recommending the block variant is wrong on macOS 26.6. This one finding would have cost hours during the build.
- (decision by coach) AD: tap IO uses AudioDeviceCreateIOProcID with a C function pointer and a global/Unmanaged context pointer, never the block+DispatchQueue variant. Consequence for design: the IOProc cannot capture Swift context, so the capture engine must pass an Unmanaged<Self> as the clientData refCon.
- (event by coach) SPIKE 2 sub-finding: tap stream format on this host is 48000 Hz, 2ch, 32-bit float, mFormatFlags=9 (IsFloat|IsPacked). Confirms the addendum's warning to query kAudioTapPropertyFormat rather than assume, and confirms Float32 handling.
- (event by coach) SPIKE 2 sub-finding: system-audio capture required NO TCC prompt and no user interaction — ad-hoc signature plus NSAudioCaptureUsageDescription plus com.apple.security.device.audio-input entitlement was sufficient on macOS 26.6. This materially de-risks PRD §12 and softens FR-42, but the inference-based UI must stay, because consent behaviour may differ on a clean machine or after a rebuild.
- (question by coach) OPEN: frame accounting in the spike came to 117600 frames over ~5s (~23.5k/s) against a 48kHz format, so the per-callback frame maths or the interleaving assumption is off by roughly 2x. Must be settled during implementation by writing an AVAudioFile and comparing its duration to wall-clock, not by reasoning.
- (constraint by coach) Swift top-level code is rejected in an SPM executable target even in main.swift under swift-tools 6.0; the entry point must be an explicit @main type in a non-main.swift file. Minor, but it cost a build cycle in the spike.
- (event by coach) SPIKE 3 (detection) PASSED: kAudioHardwarePropertyProcessObjectList returned 44 audio process objects on this host, each yielding kAudioProcessPropertyPID, kAudioProcessPropertyBundleID, IsRunningInput and IsRunningOutput. NSRunningApplication(processIdentifier:) resolves GUI app names.
- (constraint by coach) SPIKE 3 KEY FINDING — exact bundle-ID matching would silently never detect Teams. Teams exposes ONLY com.microsoft.teams2.modulehost, com.microsoft.teams2.helper (x3) and com.microsoft.teams2.notificationcenter as audio process objects; there is NO bare com.microsoft.teams2 object. Slack exposes both com.tinyspeck.slackmacgap and .helper. Prefix matching is therefore mandatory, not a nicety, and the PRD FR-11 consequence about helper processes is confirmed empirically.
- (event by coach) SPIKE 3 sub-finding: kAudioProcessPropertyIsRunningOutput demonstrably works — Spotify reported YES while playing, everything else no. IsRunningInput reported no across all 44 objects with no call in progress, which is the correct negative. Whether IsRunningInput flips to YES during a real Slack huddle cannot be verified without a live call and stays an open question.
- (decision by coach) AD: detection matches a Watched App by bundle-ID PREFIX against the audio process object list, polls on a timer as the source of truth, and treats property listeners only as a latency optimisation. Rationale: exact match misses Teams entirely (measured); listeners are documented unreliable.
- (event by coach) SPIKE 4 (WhisperKit/SpeakerKit) PASSED: argmax-oss-swift pinned exact 1.1.0 resolves and both products compile against an SPM executable in 45s with zero source changes and no extra system dependencies.
- (constraint by coach) SPIKE 4 KEY FINDING — real model identifiers are prefixed 'openai_whisper-', e.g. openai_whisper-base and openai_whisper-large-v3-v20240930_turbo_632MB. The docs summary listing bare 'base' / 'tiny' / 'large-v3-v20240930_626MB' is WRONG. WhisperKit.fetchAvailableModels() returned 22 live variants. Confirms PRD FR-17's requirement to enumerate at runtime instead of hardcoding.
- (event by coach) SPIKE 4 sub-finding: WhisperKit.recommendedModels() is a local call (no network) returning default 'openai_whisper-base' plus a device-filtered supported list; fetchAvailableModels() hits Hugging Face. So the model picker can render a sane device-appropriate default offline and only needs network to show the full catalogue.
- (decision by coach) AD: default Transcription Model is openai_whisper-large-v3-v20240930_turbo_632MB rather than the library's recommended openai_whisper-base. Rationale: base is too weak for multi-speaker meeting audio, turbo is the accuracy/speed sweet spot on Apple Silicon, and 632MB is acceptable. SM-C3 forbids defaulting to the LARGEST model, which this is not.
- (assumption by coach) FoundationModels availability not spiked — Apple Intelligence is known opted out on this host so the expected result is .unavailable(.appleIntelligenceNotEnabled). The Heuristic Backend is required regardless, so the LLM Backend is built behind an availability check and validated during the build rather than up front.
- (event by coach) Reviewer gate run. lint_spine.py: 1 low finding (a DESIGN.md token cross-reference mistaken for a template placeholder) — rephrased, now ok:true / 0 findings.
- (event by coach) Version-verification reviewer: PASS. Every Stack-table technology was verified by executing code on the target machine, including two corrections to published documentation (argmax tag 1.1.0 not 0.9.0; model ids are openai_whisper-prefixed). Three honest gaps recorded: SpeakerKit.diarize() never executed, FoundationModels availability unverified, and the no-TCC-prompt result may not generalise.
- (event by coach) Adversarial reviewer: found 4 real holes where two units obeying all 17 original ADs would still build incompatibly — Note filename ownership, Speaker Label representation (rename/merge), stage-advancement ownership, and Meeting-record field clobbering. All four were in ownership of PERSISTED state; the original ADs were strong on OS boundaries and thin here. Closed with AD-18 through AD-21.
- (decision by coach) AD-21 was the most serious fix: adapters independently persisting the whole Meeting record could silently drop the degradation flags AD-17 requires, which would present a Mic-only recording as a full one — the worst available outcome for this product.
- (event by coach) spine finalized
- (event by coach) BUILD COMPLETE: 43/43 stories implemented, ~5000 lines Swift, 9 commits, 33 tests passing, signed .app produced and verified end to end.
- (event by coach) Verified by execution inside the signed bundle: dual-stream capture (system peak 0.36), transcription, diarization correct on 6/6 lines of a scripted two-voice meeting across two models, metadata extraction with owner attribution, Markdown note with valid YAML.
- (constraint by coach) POST-BUILD FINDING: WhisperKit defaults downloadBase to ~/Documents/huggingface. This dumped 766MB into the user's Documents AND broke ModelCatalog.isDownloaded() which checked Application Support — the setup checklist's 'model ready' row would have been permanently outstanding. Fixed by setting downloadBase explicitly for both WhisperKit and SpeakerKit and migrating existing downloads.
- (constraint by coach) POST-BUILD FINDING: Whisper hallucinates stock phrases over silence with high confidence. A real run produced a phantom 'Me: Thank you.' from a microphone nobody spoke into — inventing a participant, which undermines the product's central attribution claim. Filtered via noSpeechProb plus whole-segment phrase matching.
- (event by coach) Code review found 5 further defects, all fixed: AVAudioFile data race at stop (two threads draining), truncated tail on every recording (single-chunk flush), dictionary mutation during iteration in the 2s detection poll (crash risk), and an invalid strict-weak-ordering comparator.
- (event by coach) Retrospective written. Verdict: accept with three items requiring the user — live-huddle detection test, PRD §14 assumption review, and the signing-certificate decision.
- (override by user) USER CORRECTION to AD-11: 'My microphone is not always me. I might have the mic but be in a meeting room with others.' This invalidates the structural guarantee as written — in a conference room the mic captures several in-person people, and attributing all of it to the Local Speaker puts colleagues' words in the user's mouth, which is strictly worse than an anonymous label.
- (decision by coach) AD-11 REVISED. What survives: the mic/system split is still structural and still valuable — mic = people physically in the room, system = remote participants. What is dropped: the claim that the mic stream IS the user. New rule: diarize BOTH streams. If the mic yields exactly one speaker, label it the Local Speaker (solo desk work, the common case, and the inference is then safe). If it yields more than one, label them In-room 1..N and assert nothing about which is the user; the existing rename + SpeakerProfile machinery names them once and remembers the voice thereafter.
- (decision) AD-22 added: a backend reports Capability (.ready/.needsDownload/.needsKey/.blocked with reason+remedy), never Bool. Binds FR-58. Prevents the exact failure that produced this increment - the app knew why it could not summarise and had nowhere to put that knowledge.
- (decision) AD-23 added: backend precedence is one pure function (user selection, capabilities) -> (backend, why), unit-tested over every combination. Prevents two settings disagreeing silently and lets the pane state which backend runs NEXT rather than only which is selected.
- (decision) AD-24 added: egress is a single chokepoint whose input type cannot express audio, a file URL or a Speaker Profile, so 'audio never leaves' is enforced by the signature not by review. Refuses to send without a stored key AND a per-Meeting consent token. Keeps NFR-6 verifiable by having one place to look.
- (decision) AD-25 added: one download mechanism and one storage root for both kinds of model. Spike verified HubApi(downloadBase:) honours an explicit root, so this is configuration rather than a fight with the library.
- (decision) AD-26 added: AD-14's serial ML rule extends to summarisation - transcription model unloaded before a summarisation model loads. Achievable because AD-8 orders summarisation after transcription, but stated as a requirement rather than left as an accident.
- (decision) AD-27 added [ADOPTED]: the Metal toolchain is a detected prerequisite. mlx-swift ships no prebuilt metallib. Binds the build too - build-app.sh must copy SPM resource bundles into the .app, which it currently does not.
- (change) AD-12 amended: the Metadata port goes from two implementations to four, and the floor stops being a summariser - so the port splits into MetadataBackend (all four) and Summarizing (three). AD-13 extended to cover the summarisation model list.
- (event) Headless -H update, increment 4: voice enrolment. Inputs: PRD FR-62..FR-65 + amended FR-21/FR-25/FR-46, both UX spines, the mic-isolation spike, and the threshold calibration.
- (constraint) CALLER-BINDING: the identification mechanism must be plain maths on a fixed-length embedding with no Apple-specific dependency. Apple-only techniques may exist as adapters behind a port, never as the mechanism. This is the stated reason Voice Isolation was rejected despite being the cheapest effective option in the spike.
- (constraint) CALLER-BINDING: the match threshold is a calibrated constant and not a setting. CALLER-BINDING: enrolment audio is destroyed once the embedding is derived. CALLER-BINDING: SpeakerDirectory is reused, not duplicated.
- (version) No new dependency. SpeakerKit.DiarizationResult already exposes nearestSpeakerCentroid(to:)->(speakerId,distance) and centroidCosineDistance(between:and:) via argmax-oss-swift exact 1.1.0, already linked. Per-meeting centroids already written to centroids.json. Audio already 16 kHz mono 16-bit, which is what the diarizer wants on read.
- (decision) AD-28: VoiceEmbedding is a port in Services; the comparison is Foundation-only code in Core operating on [Float]. Binds FR-62/63/65. Prevents the identification path acquiring a platform dependency — the one place the user explicitly asked for portability. An adapter may be as Apple-specific as it likes; the port signature and the arithmetic may not name a single Apple type.
- (decision) AD-29: a fingerprint carries its producer identifier and dimension, and a cross-producer comparison returns NO INFORMATION rather than a distance. Prevents an embedder swap silently comparing incompatible vectors — the worst available failure, because a wrong distance is indistinguishable from a right one. cosineDistance's nil-on-length-mismatch already does half of this by accident; the identifier makes it deliberate.
- (decision) AD-30: .local is assigned in exactly two ways and no third exists — structurally (one mic voice) or by an enrolment match that is both within threshold and unambiguous. SpeakerDirectory.match EXCLUDES the enrolled profile from its candidate set, so the FR-25 passive path can never put the user's name on a voice. One writer of the user's identity.
- (decision) AD-30 ambiguity rule: two in-room clusters both within threshold and within 0.10 of each other yields NO identification. Chosen over picking the nearer one because a claim the data does not support is the failure AD-11 was amended to prevent; the measurement says in-room voices sit >=0.596 apart so this should be rare, and when it fires it is most likely a diarizer split of the user's own voice.
- (decision) AD-31 [ADOPTED]: one threshold constant, 0.35, with the measurement and date in the comment beside it. Not in Preferences, not in UserDefaults, not reachable from any pane. Prevents the number drifting by opinion and prevents the one setting that would let a user break attribution invisibly.
- (decision) AD-32: enrolment audio is transient. One operation records, embeds and deletes, with the deletion in a defer so it also runs on failure and cancellation. Never enters a Meeting directory, never becomes a Meeting record, never reaches the Notes Folder. A re-record replaces rather than averages, so no history of samples exists to leak.
- (decision) AD-11 amended in place, ID kept: identity now has a third resolution path (an enrolment match) while place stays structural and unchanged. The amendment is one clause plus a pointer to AD-30; the rule it replaces is not deleted, because 'no voice may be claimed as the Local Speaker' still governs the no-enrolment case.
- (decision) Pipeline.assign gains one parameter naming which mic cluster is the local voice, and nothing else changes. The structural rules — mic can only become in-room, system can only become remote, unplaceable mic speech with several voices becomes unidentified — are untouched. Enrolment decides an input to the function, not the function.
- (change) Frontmatter binds corrected: it read FR-1..FR-48 and the PRD has had 61 FRs since increment 3 and now has 65. Stale since increment 2 and never noticed.
- (change) The pipeline diagram's diarize node read 'Diarize system stream' and the code has diarized both streams since AD-11 was amended. Corrected with the enrolment lookup shown as a read from SpeakerDirectory rather than a new stage — AD-8's stage list does not grow.
- (change) Deferred updated: 'Speaker Profile matching algorithm' is now decided (AD-31, calibrated) rather than pending PRD Q4. Added deferrals for macOS input voice processing (setVoiceProcessingEnabled — works but changes 1 channel to 3 deinterleaved and which channel carries the clean signal is unmeasured) and per-device fingerprints (PRD Q19).
- (override) HEADLESS OVERRIDE: reviewer gate run as lint_spine.py plus an inline self-check rather than parallel reviewer subagents. No finalize_reviewers are configured, and the caller's environment forbids spawning agents. Findings applied in place; nothing deferred silently.
- (event) Reviewer gate: lint_spine.py clean (0 findings) before and after. Rubric walker run INLINE, not as an independent subagent — reviewer-gate.md says an inline self-check is weaker because a fresh context finds what the author talks past, and that weakness is recorded rather than glossed.
- (change) GATE FINDING (fixed): AD-30 did not say WHERE the enrolment lookup happens, so two implementations could reasonably add a pipeline stage instead of passing a value. Now fixed: the lookup runs inside the existing diarize stage and its result reaches attribution as data; adding a stage is a violation, not a choice.
- (change) GATE FINDING (fixed): concurrency was silent for the new service. VoiceEnrolment is @MainActor like TestPlayground, SpeakerDirectory is already an actor, and embedding runs on the ML serial executor so an enrolment during transcription queues rather than contending.
- (change) GATE FINDING (fixed): the operational dimension was silent. Added a 'Build and Delivery Envelope, Increment 4' section stating explicitly that nothing changes — no build-script change, no dependency, no entitlement, no model download, and one backward-compatible file shape. Increment 3's AD-27 did bind the build, so 'nothing changes' is a claim worth making rather than inferring.
- (event) spine finalized — AD-11 amended in place, AD-28..AD-32 added, no ID renumbered. binds corrected to FR-1..FR-65. updated 2026-09-01.
- (decision) Increment 5 begins: the app must be installable by someone other than its author. Repo published at github.com/NiklasLuettringhaus/Minutes (personal account), trunk-based with short-lived feature branches, CI on every PR.
- (decision) User data is now structurally excluded from the repository, not merely absent: Scripts/check-no-user-data.sh runs as a pre-commit hook and as a CI job over the whole tree. It rejects by shape as well as name — a run of 32+ floats in one array is a voice embedding whatever the file is called. Keying on the name alone was tried first and let a renamed speakers.json through, because the real key is 'centroid'.
- (event) MEASURED on this machine: Homebrew 6.0.20 has removed --no-quarantine entirely (absent from 'brew install --cask --help', rejected as an argument). Consequence: a cask-installed ad-hoc signed app cannot pass the Gatekeeper check that quarantine triggers, so the one-command Homebrew install requires Developer ID signing plus notarization. This is not a preference; it is the mechanism.
- (decision) Commit history rewritten before first push to carry the repo owner's personal-account noreply address instead of a work email. Verified content-identical: the tree hash of HEAD was unchanged by the rewrite. One document cited a commit hash and was repointed.
- (event) VERIFIED by reading the code, not inferred: AppState.lastError is written at SessionCoordinator.swift:38,48,57,61 and read by exactly one place, App/SelfTest.swift:43 — a CLI path. No file under Sources/Minutes/UI/ reads it. A failed recording start is therefore silent in the GUI, and MinutesError's remedy strings are unreachable from it.
- (event) VERIFIED: SystemTapCapture.stop() computes gotAudio = (peak > 0.0001) || duration > 0.25. The doc comment immediately above calls producedAudio 'the only evidence we can have that system-audio capture actually worked' (FR-42). The duration clause makes 0.25s of pure silence sufficient, so the evidence is vacuous. TestPlayground has the same defect on the mic side: micHadAudio = (streams.micURL != nil) — the file existing, not audio in it.
- (event) VERIFIED: ModelStorage.adoptLegacyDownloads() runs unconditionally at every launch (MinutesApp.swift:78) and removes the whole ~/Documents/huggingface tree when only its whisperkit-coreml subtree is empty. Anyone with other HuggingFace models cached there loses them silently.
- (event) VERIFIED: nothing in Sources/ reads its own CFBundleShortVersionString; Info.plist hardcodes 1.0/1. There is no About surface and no update check. Every colleague's bug report would say 1.0.
- (event) VERIFIED: the default notes folder is ~/Documents/Minutes (Preferences.swift:145). With iCloud Desktop & Documents sync on — common on managed fleets — transcripts upload, while SummariesPane.swift:83 asserts 'Nothing about your meetings leaves this Mac'. Also LoginItem writes ~/Library/LaunchAgents/dev.niklas.minutes.login.plist, which survives deleting the app.
- (override) Owner's decision: drop Homebrew, do not buy the Developer ID yet, ship a GitHub Release archive plus a quarantine strip. This overrides AD-34's rule that no release depends on a Gatekeeper bypass; AD-34 is amended in place to say so rather than left contradicting what ships. FR-73 (consent survives an update) is therefore not met by this path and stays open.
- (event) MEASURED: Command Line Tools alone CANNOT build this app — the FoundationModels @Generable/@Guide macro plugin ships only inside Xcode (present under Xcode's platform dirs, absent from CLT). The framework itself IS in the CLT SDK; only the macro plugin is missing. Exactly one file imports FoundationModels. Stubbing it made a CLT-only release build succeed, so the 3.7 GB Xcode prerequisite is caused by one optional feature that already has a deterministic fallback.
- (event) MEASURED on a fresh clone from GitHub: clone 1s / 6.2 MB; clean release build + bundle + ad-hoc sign 67s; 1.1 GB of build artifacts. First CI run green on all three jobs (user-data guard, build and test, bundle and sign) on macos-15.
- (event) MEASURED, and it corrects an earlier conclusion: curl does NOT set com.apple.quarantine (zero attributes on the downloaded zip and the unpacked app), and the curl-installed ad-hoc app launches normally — verified by 'open -a', pid alive, no Gatekeeper log lines. The identical bundle with a browser-style quarantine flag written by hand is BLOCKED. So Gatekeeper was never the obstacle for a curl installer; Homebrew was, because Homebrew applies quarantine deliberately. Dropping Homebrew removed the problem instead of requiring a bypass. The installer's xattr call is defensive and a no-op on the documented path.
- (decision) v0.1.0 released: 10 MB arm64 archive plus sha256, published from a tag whose annotation is the release notes. Version now derived from the tag (AD-33) and verified end to end: the installed bundle reports 0.1.0 (52) / v0.1.0 / release=true via --doctor.
- (event) MEASURED (spikes/calibration-audio-evidence-2026-09-02.md): 26 real streams. The shipped evidence rule accepted all 26, including 32 min and 13 min of pure digital silence (peak exactly 0.0000). The calibrated rule accepts 23. Second defect found while implementing: StreamFileWriter.peak DECAYS (max(peak*0.85, localPeak)) because it is the UI meter, so even the signal half of the old test was near-zero by stop() on any meeting that ends quietly — the duration clause was masking it. AudioEvidence adds a non-decaying peakEver and a non-silent frame count.
- (decision) AD-36 implemented: AudioEvidence in Core (Foundation only), silenceFloor 0.0005 and minimumSignalSeconds 0.5, both bounded by measurement and by FR-47's five-second test. Both conditions required — a peak with no duration is a pop, duration with no peak is the original bug.
- (event) Code review of stories 11.1/11.2: nine findings, two HIGH, all addressed. The worst was that the new system-audio error diagnosed a cause the app cannot determine — a working tap with nothing playing delivers exact digital zeros, identical to a revoked permission — so it told users to reset TCC after any recording where nothing played. Fixed by naming both causes and adding Remedy.runAudioTest, which discriminates, instead of a settings pane that assumes. The second HIGH: the reviewer reverted peak:peakEver to the decaying meter and all 174 tests still passed, proving the rule was pinned but the wiring was not. StreamFileWriterEvidenceTests now drives the real writer and fails on that revert.
- (decision) Core purity is now a test rather than a comment (CorePurityTests): every file in Sources/Minutes/Core may import only Foundation or os. AD-28's rule was prose in a doc comment and therefore unenforced.
- (decision) AD-39: a Note carries its Meeting ID in frontmatter and the link is resolved through a NoteLocating port. Binds FR-77/FR-78/FR-53; prevents a filename serving as an identity, which is what orphaned a real Note permanently
- (decision) AD-39 runs only when the recorded path is absent — zero folder reads in the normal case; a single match persists through MeetingStore, ambiguity and absence persist nothing
- (decision) AD-9 amended in place rather than weakened: identity is not content, and the identity reader's return type makes a body field unrepresentable
- (decision) AD-9's second amendment: 'edits will be overwritten' was the consequence only while the app could not recognise its own bytes; AD-41 removes that, and the unmet 'must be stated in the UI' clause becomes FR-81
- (decision) AD-40: two names on the record — the linked file and the last name the app wrote. Divergence means the user owns the name. No didUserRename flag, because a flag can fall out of sync with the filesystem and the comparison cannot
- (decision) AD-18 amended in place: its authority holds only while the two names are equal; AD-40 inverts it otherwise
- (decision) AD-41: a digest of the exact bytes written, persisted with the filename in the same update. Never compare against a fresh render — the renderer changed in four increments, so every old Note would report as edited
- (assumption) AD-41 migration uses note mtime vs record write time for the fifteen pre-digest Notes. Weaker than a digest (blind to a timestamp-preserving edit) but evidence rather than assumption; measured: none of the fifteen is modified after its record
- (decision) AD-42: trashItem for anything the user has seen, removeItem only for temp and staged files; a Trash failure is an error with no fallback to unlinking
- (constraint) AD-42 is retrospective, not precautionary: removeItem on a Meeting directory already destroyed a real recording, with an empty Trash afterwards
- (decision) AD-43: enumerate only files carrying a Minutes identity marker. FR-79 is the single exception, where the user names a file explicitly and the app states what a rewrite would do to it
- (question) whether started_at remains a sufficient fallback key for pre-stamp Notes once two Meetings can start in the same second (PRD §13 Q22)
- (event) spine updated for increment 7: AD-39..AD-43 added, AD-9 and AD-18 amended in place, no ID renumbered; lint_spine clean, 0 findings
- (constraint) MEASURED 2026-09-03: 7 of 16 recordings have system.wav declaring 16 kHz at a true rate of 8000 Hz (ratio exactly 2, five files) or 5333 Hz (ratio exactly 3, two files); all seven had a Bluetooth input device, mic streams unaffected in every case
- (constraint) mechanism located in source: kAudioTapPropertyFormat is read once immediately after AudioHardwareCreateAggregateDevice; the IOProc is correct and records exactly the bytes delivered; StreamFileWriter's converter then resamples as if the ring arrived at the declared rate
- (decision) AD-3 amended in place rather than superseded: its own Prevents clause already named this exact failure, so the rule was followed and was insufficient. Querying the format is necessary; it does not establish that the rate stays true
- (decision) AD-36 amended to widen its scope while keeping 'duration never contributes' intact — 'was audio captured' and 'are the samples at the rate they claim' are different questions, and only the second uses duration, as a denominator rather than as evidence
- (decision) AD-44: the capture rate is observed, not declared. The writer compares consumed frames against elapsed time; a disagreement is a named failure, never a silent resample. Runs while recording, because a two-hour meeting is too expensive to discover afterwards
- (decision) AD-45: capture evidence gains a second independent component — samples/elapsed against the declared rate. Catches the whole class (rate misread, dropped buffers, converter misconfiguration, clock drift) because all of them present identically
- (decision) AD-45 records untrustworthiness on the Meeting rather than as display state, and per stream: the microphone being sound says nothing about the system stream, and in every observed case the microphone was sound
- (decision) AD-46: no title, tag or summary is derived from a stream that failed its evidence check. This is the clause that addresses why the defect looked fine — a confident auto-title on fabricated text disguised it for three days
- (decision) AD-46 keeps the transcript rather than discarding it: the samples are the user's, and the Note states which stream is untrustworthy in the same voice as every other degradation (AD-17, FR-7)
- (decision) what to DO about a live rate disagreement — correct the converter or stop the stream — is left to story level; that it must not proceed silently is fixed here
- (event) spine updated for increment 8: AD-44..AD-46 added, AD-3 and AD-36 amended in place, no ID renumbered; lint_spine clean
- (decision) AD-47: the System Stream is authoritative for far-end speech; the Mic Stream's correlated copy is excluded before the Transcription Model AND the Diarizer, never after. Binds FR-89..FR-91. Prevents both duplicated text and phantom attendees.
- (decision) AD-48: echo is detected, never cancelled. Coherence bounds ANY linear canceller at 8.7-10.6 dB median against the 20-40 dB needed; revisiting requires a new measurement, not a new library.
- (decision) AD-49: exclusion never modifies audio on disk; verdict, delay and excluded proportion are recorded so the decision is recomputable after a threshold change. An unprocessed recording reads as unknown, never clean.
- (decision) AD-50: an accuracy claim requires a measurement taken through the shipping port via a CLI, so the harness cannot drift from the product. Pool errors over pooled words; a single session is not a measurement (+-8 point swing).
- (decision) AD-51: the audio clock (mSampleTime/mHostTime from the IOProc) is the time base for rate verification; the wall clock is never a rate's denominator. Sample-time gaps become recorded discontinuities. AD-4's session clock unchanged for Utterances.
- (decision) AD-52: confidence and gaps join the Transcribing port contract; absent means unknown and must never read as zero.
- (change) Amended AD-3 (the session clock was the wrong clock), AD-11 (place is structural for the RETAINED Mic Stream, not the raw one), AD-36 (evidence of capture is not evidence of capture from a distinct source).
- (constraint) FluidVoice is GPL-3.0. Its techniques informed AD-51 and AD-52; no code was copied, and none may be.
- (event) spine updated for increment 9; lint_spine clean, 0 findings
- (override) Story 16.5 REFUTED by measurement: muting Echo before clustering made the in-room voice count WORSE (5->7, 6->6, 3->5) because muting fragments continuous speech and the clusterer splits one voice. Removing audio to fix a speaker count is now forbidden; FR-100 replaces it with far-end embedding exclusion.
- (override) AD-48's blanket prohibition on cancellation was too broad. The measurement (8.7-10.6 dB linear bound) supports forbidding POST-HOC LINEAR cancellation only. The field's answer is linear + non-linear residual + double-talk detection, at capture, which is FR-99.
- (decision) Apple's VoiceProcessingIO is ruled out on structure, not preference: its echo reference is the app's own output bus, so it cannot cancel audio played by the conferencing app. Recorded so it is not rediscovered.
- (decision) Echo handling is gated on the OUTPUT DEVICE (FR-98) rather than on a correlation threshold. Every clean recording was on headphones and every affected one was not, so the fact is available for free where FR-89 spends a search to estimate it.
- (decision) VAD segmentation rejected for this product: measured in the literature to have no significant effect on DELETION rates, and deletions are 290 of our 405 baseline errors. It fixes insertions and hallucination, which increment 8 already addressed.
- (decision) Guided source separation rejected: an array technique. Two channels give a fraction of seven-channel benefit (50.4% / 21.8% relative WER reduction going 2->7 channels), and ours are not even a coherent array.
- (constraint) CHiME-8 DASR: the winning pipeline is diarisation -> separation -> ASR, and diarisation gains exceed ASR gains. Our pipeline is the reverse order. Reordering is noted as a candidate, not adopted on one paper summary.
- (assumption) FR-99's non-linear residual stage is expected to clear the bar the linear stage could not, but that is UNMEASURED on our recordings. FR-90's post-hoc rule stays until the harness says otherwise.
- (decision) AD-53: the two Streams' offset is the difference of their first samples' host times, measured for EVERY dual-stream Session including clean ones. Replaces a correlation search that only works on a recording that already has an echo.
- (decision) AD-54: the output-device fact is three-valued (loudspeakers/headphones/unknown). Only headphones disables Echo handling; unknown leaves FR-89's detector in charge. A boolean would be wrong on AirPods or on a Bluetooth loudspeaker, which report the same transport.
- (decision) AD-55: cancellation runs on samples in flight to the file or not at all. Draws the line AD-49 was about to be misread across: AD-49 governs exclusion over audio that exists; cancellation is part of producing it. Nothing may cancel a file already on disk.
- (decision) AD-56: a cluster is ruled out of the room by embedding distance to a System Stream centroid, and any cross-stream threshold must be stated with a CROSS-STREAM measurement. AD-31's 0.35 was calibrated same-channel only.
- (change) Amended AD-44 (its tolerance and settling period are the wall clock's and the wall clock is now the fallback), AD-49 (scope stated: exclusion, not production), AD-50 (the pooled figure is produced by the harness after the rule failed against itself), AD-51 (rate and continuity are two measurements; tolerance computed per measurement).
- (event) spine updated for increment 10: AD-53..AD-56 added, AD-44/49/50/51 amended in place, no ID renumbered; lint_spine clean, 0 findings
