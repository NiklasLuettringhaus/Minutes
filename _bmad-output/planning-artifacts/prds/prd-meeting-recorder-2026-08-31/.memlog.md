---
topic: Minutes — local-first macOS meeting recorder PRD
goal: Implementable PRD driving epics/stories for a same-day build
updated: 2026-09-07T13:57
---

- (event by coach) Headless create. Input: brief.md + addendum.md from brief-meeting-recorder-2026-08-31. No interactive discovery possible; user is away until end of day.
- (decision by coach) Stakes read as personal/internal utility, not launch. PRD targets ~5-8 pages: lean on business sections, heavy on FRs because these feed epics/stories and a same-day build directly.
- (decision by coach) Adapt-In clusters pulled: Cross-Cutting NFRs, Constraints and Guardrails (privacy/cost), Performance Budgets, Aesthetic and Tone (brief demands a 'small clean UI'). Invented section 'Platform, Permissions and Signing' — TCC consent keyed to code signature with no Developer ID present is the product's single largest risk and no template cluster names it.
- (decision by coach) Dropped Monetization, ROI, Stakeholders/Approvals, Rollout, Compliance/Regulatory, Integration and Dependencies: single-user local tool with no business model, no external systems and no regulator.
- (decision by user) MID-RUN USER DIRECTIVE (with FluidVoice screenshot): 'I like the way fluid voice sets up the models. Do something similar.' FluidVoice pattern to adopt: single window with grouped sidebar (Configure/Use/Activity/Help); a 'Quick Setup' checklist card whose rows carry state icon + title + one-line explanation + right-aligned control (dim 'Done' pill when satisfied, actionable button when not); completed rows de-emphasised; 'Run Onboarding Again' affordance; and a 'Test Playground' card that proves the pipeline end-to-end in-app.
- (decision by coach) Adopting the Test Playground is the highest-value part of the directive: macOS exposes no API to query system-audio-capture permission (see FR-42), so an in-app record-and-transcribe test converts an unanswerable question into an empirical one. Promoted from nice-to-have to a requirement.
- (change by coach) IA consolidated as a consequence: replaced the planned separate Settings window + Library window with ONE sidebar window (Setup / Transcription / Meetings / Detection / General). Net surface area roughly unchanged, so SM-C2 is respected; two windows collapsing into one is a simplification, not an addition.
- (change by coach) Added FR-46 (Setup Checklist), FR-47 (Test Playground), FR-48 (re-run onboarding), and section 'Information Architecture and Visual Design' capturing the sidebar shape and the checklist row anatomy.
- (event by coach) Reviewer gate run: rubric walker across all 7 dimensions -> review-rubric.md. Verdict: implementable and well-grounded; 2 high findings (assumption density inappropriate for a green-light PRD; 48 FRs vs the user's word 'small'). Fixed in place: added §6.3 Build Order tiers (all 48 FRs assigned, verified programmatically), stated the thesis in one sentence in §1, marked SM-1 provisional pending measurement, plain-worded FR-20, folded §9.3. Assumption density cannot be fixed without the user — escalated in addendum §5.
- (event by coach) PRD finalized
- (change) FR-49/FR-50 added: elapsed time on the menu bar label and an animated Recording indicator. FR-4 already required elapsed time inside the open menu; the user wanted it visible without opening the menu, which is a different requirement.
- (change) FR-51 added: remembered voices are listable, renamable and individually forgettable. FR-25 created Speaker Profiles but gave the user no way to see or curate them, so a wrong match was only correctable via a meeting that happened to contain that voice.
- (change) FR-52 added: the Metadata Backend is named and selectable in Settings. FR-30 recorded provenance in the Note only, so a user reading the app could not tell what produced a summary, or that Apple Intelligence being off was the reason.
- (change) FR-53 added: Note presence is verified against the Notes Folder and a missing Note is rewritable. Observed defect: 7 Meetings recorded stage=written with a noteFilename while only 2 files existed on disk, and nothing reconciled the record against the folder.
- (change) FR-54 added and FR-40 amended: the Library refreshes from disk, and Delete is reachable without discovering a context menu. The user asked to manage meetings in-app and did not find the existing context-menu delete.
- (decision) Note-to-record reconciliation is read-only about the Note: a missing Note is reported and offered for rewrite from meeting.json, never inferred back into the record. Keeps AD-9 (the Note is a projection, never parsed back) intact.
- (decision) The Recording animation is confined to the Recording state. FR-5's feature NFR already forbade animating while Idle; that constraint is now explicit rather than implied.
- (assumption) A per-voice rename in the voices list renames the Profile only, not the Speaker Labels in already-written Meetings. Retro-editing history was not requested and would rewrite Notes the user may have edited.
- (override) FR-27 amended: 'prefer the LLM Backend' is now a default the user may override via FR-52. Reverses the implied rule that availability alone decides.
- (override) 6.2 corrected: 'Recovery UI for crash-orphaned Sessions' was listed out of scope for MVP but has been built. The original rationale understated the symptom - an interrupted Session span a spinner forever and read as a hang. Covered by FR-39's surface; no new FR.
- (event) Reviewer pass (inline, hobby-stakes): 54 FRs, every one tiered in 6.3, verified programmatically. Two conflicts with prior decisions found and reconciled rather than deferred. 4 new open questions raised, all measurement-shaped and none blocking.
- (event) bmad-review run on the proposal 'gate summaries on a real LLM; user supplies an API key'. Lenses: adversarial (14 findings) + edge-case-hunter (13 unhandled paths). Verdict: split the proposal. Report at review-llm-key-proposal.md/.html.
- (decision) Decision A (gate the summary on a real model) recommended for acceptance and can ship independently: omit the summary/decisions/actions sections entirely rather than filling them with sentence extracts, per 9.3. Requires an FR-26 amendment so the title guarantee survives while the summary becomes conditional.
- (decision) Decision B (where the intelligence comes from) NOT taken pending evidence. Blocking finding: FoundationModelsBackend is already built and the M5/24GB machine is eligible - availability reports appleIntelligenceNotEnabled and opted_out_buddy=1, i.e. Apple Intelligence was declined at Setup Assistant. Enabling it costs nothing and breaks no NFR. Cloud key is option 3 of 3 and the only one that violates NFR-1/NFR-6/9.2.
- (assumption) Reviewer assumption: the weak summaries are partly a transcription problem, not only a summarisation one - 'preaching page' is a Whisper error on the base model. Recommend re-transcribing one meeting with large-v3-turbo before any Decision B work, since a wrong transcript makes every option produce a fluent wrong summary.
- (direction) User decision: do NOT rely on Apple Intelligence, but keep it as one option. Add a summarisation-intelligence settings pane mirroring the transcription model picker, offering a local downloadable LLM or a user-supplied key. Run the full BMAD flow, stop before implementation.
- (event) Spike run before requirements: MLX local LLM is feasible (mlx-swift 0.31.6 / mlx-swift-examples 2.29.1 compile and link, HubApi downloads into our own models dir with real progress). Hard blocker found: Xcode Metal Toolchain component is uninstalled, so no metallib is built and MLX cannot run a single token. Report at planning-artifacts/spikes/spike-local-llm-2026-08-31.md
- (change) Increment 3 PRD update applied: FR-55 to FR-61 added, section 4.10 Summarisation Intelligence created, FR-26/FR-27/FR-52 amended, glossary Metadata Backend expanded from 2 to 4 members plus Summarisation Backend and Prerequisite, Tier 5 build order added, 5 open questions and 5 assumptions recorded. 61 FRs, all tiered.
- (override) NFR-1 amended for the first time in the project: 'no audio, Transcript or Metadata transmitted anywhere, ever' becomes 'on-device by default; egress only by explicit per-use consent'. Audio and Speaker Profiles stay absolute never-transmit invariants. Done in the open because FR-59 makes a Transcript transmissible - the alternative was shipping code that contradicts a standing NFR.
- (override) NFR-6 amended: the egress claim is now conditional, so it is stated more precisely - zero traffic with no Remote Backend configured, traffic only to the configured endpoint when a Meeting was sent, never audio.
- (override) 9.2 zero-cost renegotiated rather than broken: it remains the constraint on every path shipped enabled; the Remote Backend is off by default, must never become the default, and FR-60 requires cost shown before it is incurred.
- (decision) FR-27 demoted: Apple's model is one entry in a list, not a privileged tier, on the user's explicit instruction not to rely on it while keeping it available. The demotion is itself the requirement.
- (decision) Tier 5 ordered so FR-55 (no summary unless something real produced it) ships first and standalone, and FR-59 (remote key) ships last - it is the only transmitting part and carries an unresolved consent finding.
- (change) Story 9.1 built: HeuristicBackend now returns title and tags only, no summary/decisions/actionItems. Note is frontmatter + Transcript with speaker IDs. Extraction functions retained unused and still unit-tested - re-enabling is a one-line change, which matters because action-item extraction was the best of the three.
- (change) Audio storage changed to 16 kHz mono 16-bit via AVAudioConverter in StreamFileWriter. Measured 532 KB/s -> 64 KB/s, 8.4x smaller; a 45-min meeting goes 1.47 GB -> 176 MB. Nothing consumed the old resolution: both engines and the diarizer resample to 16 kHz mono on read. Added a performance budget so a future regression has something to violate.
- (assumption) Tags kept in the note frontmatter under story 9.1. The user said 'titles and pure transcripts thats it', which most directly names the visible prose sections; tags are frontmatter metadata for filtering and story 9.1 as approved retains them. Trivially removable if that reading is wrong.
- (change) First real meeting (2026-09-01, 8-person Slack huddle) exposed three defects. Detection worked but the notification hid Record behind 'Options', so the panel is now the primary prompt and the notification a durable record. Debounce 4.0->2.5s, poll 2.0->1.0s.
- (change) Auto-stop never fired because the session was started manually and triggeringApp was nil - FR-14 restricted auto-stop to prompt-started sessions, which had been implemented as 'do nothing at all'. A manual start now adopts whichever watched app holds the input, and a manual session gets a stop PROMPT (StopPanel) while a detected one still auto-stops.
- (change) Mic mute added (menu bar, while recording). Muting in Slack stops Slack's outgoing stream, not the macOS input device, so Minutes kept recording the room. Silence is written rather than frames dropped, so both streams stay on one clock (AD-4).
- (assumption) Measured on the real meeting: 723 of 1501 transcript words (48%) were the conversation beside the user, not the meeting. Minutes 4-8 contain zero far-end speech and only in-room speech, which most likely means the huddle ended around minute 4 and recording continued on the room - tying the missing stop prompt directly to the polluted transcript. Needs formal PRD capture as an increment-4 requirement set.
- (event) Headless -H update, increment 4: voice enrolment. Driven by the first four real meetings, the mic-isolation spike, and a new threshold calibration run on those meetings' centroids.
- (decision) Enrolment scoped to the USER'S OWN voice only. Enrolling other people stays deferred and nothing in FR-62 builds toward it — the reversal of §6.2 is one line for one person, not a scope reopening.
- (change) FR-62 added: record a 20-30s sample of the user's own voice from the Setup Checklist; nothing stored until a sample exists; sample audio destroyed as soon as the fingerprint is derived; mic stream only, so it can never sample the far end.
- (change) FR-63 added: the enrolled voice identifies the user among several in-room voices. Changes WHICH mic cluster becomes the Local Speaker and nothing structural — place stays a fact (AD-11).
- (change) FR-64 added: the enrolled voice is visible in the remembered-voices list as a distinct kind of entry, deletable in one click, and 'Forget all' names it. Without this the increment would breach §9.1 by storing a fingerprint the user cannot see or remove.
- (change) FR-65 added: the identification is disclosed and correctable, and no threshold/VAD/cluster-count dial is exposed. Ship one value, expose corrections — a wrong threshold puts the wrong name on someone's words with no way to notice.
- (decision) THRESHOLD CALIBRATED, not chosen: 0.45 -> 0.35. Measured on 24 centroids from 5 real meetings — same in-room voice 0.058-0.248 across four independent recordings, different in-room voices >=0.596. 0.35 keeps 41% headroom and sits 1.7x below the tightest genuine impostor. Recorded in spikes/calibration-speaker-threshold-2026-09-01.md.
- (decision) Two 'impostor' pairs at 0.128 and 0.373 were proven to be ONE person heard on both streams (83-93% temporal overlap, identical text) — a colleague in the room who was also on the huddle. Excluded from the error count; the embedding was right, the naive population split was wrong.
- (decision) 0.25 rejected despite zero measured false matches: only 0.002 headroom over the observed same-voice maximum of 0.248, so a different room or microphone breaks enrolment. A missed match costs one rename; a broken feature costs the increment.
- (change) PRD §13 Q4 answered for in-room voices and narrowed for remote ones. Remote same-speaker distances reach 0.248 and different-speaker distances start at 0.254 — the populations touch and NO threshold separates them. Recorded as a measured limit rather than fixed.
- (change) FR-21 amended: an enrolment match makes one in-room voice the Local Speaker; unplaceable mic speech is unidentified in-room speech, never the user (the 14-utterance defect from the first real meeting).
- (change) FR-25 amended: passive learning applies to other people and fails for the user, because it needs a correct label to start from and several mic voices provide none. The enrolled profile is excluded from FR-25's automatic name application.
- (change) FR-46 amended: the enrolment row is part of the existing Setup Checklist with the existing row anatomy. A second onboarding style is a defect.
- (change) §6.2 reversed for the user's own voice only, with the reasoning written into the scope line rather than left in a commit message.
- (change) §9.1 extended with four non-negotiable rules for the enrolled voice, and the honest word 'biometric-adjacent' defended in place: a 256-number embedding cannot be played back but does identify a person.
- (change) SM-2 amended: attribution trustworthiness used to be purely structural, so any failure was a bug by construction. With an enrolment path that can be wrong, the metric splits — a claim must be right when made, and an honest refusal to claim is not a miss.
- (change) §6.3 Tier 6 added, ordered measurement -> mechanism -> surface. The calibration is listed as step 1 and marked as something that should have preceded any build.
- (override) HEADLESS OVERRIDE: no reviewer gate menu, no memlog walk with the user, no input-reconciliation subagents. Change signal was fully specified by the caller; every inference is in §14's increment-4 block and every gap in §13 Q18-Q21.
- (assumption) Inferred without confirmation: the specific sample length inside the given 20-30s range; the ambiguity rule refusing rather than picking the nearer voice; re-record replacing rather than averaging; the enrolled profile applying no name; the row sitting last in the checklist.
- (change) §11 gained two enrolment budgets, both labelled targets not measurements: <10s to derive a fingerprint from a 25s sample (unmeasured, owned by Q18) and free comparison at any meeting length (arithmetic, safe to assert).
- (event) PRD increment 4 applied. 65 FRs, none renumbered. status stays final; updated 2026-09-01.
- (change) FR-50 WITHDRAWN on the user's instruction after living with the shipped build: 'revert the change that made the recording icon animated. the solid red circle was better.' Recorded as a withdrawal rather than deleted — the increment-2 reasoning was sound and use went the other way. Retires §13 Q10 unanswered and moot.
- (decision) The icon is now one image per state, built once, rather than rebuilt per 0.5s tick. The pulse was the only reason it was ever rebuilt, so withdrawing it is strictly cheaper against NFR-3 than the exemption FR-50 needed.
- (change) New §11 budget: editing a title or a speaker label must not re-render the transcript. Added because the defect was invisible in every small test meeting and only appeared on a real 598-utterance one.
- (change) increment 7: added §4.12 (FR-77..FR-83) — the Note is the user's file; driven by a user renaming a Note in Finder and the app then losing the meeting
- (decision) the Note carries the Meeting ID in its own frontmatter (FR-77) — the link stops being a filename, because a filename is the property a user is most likely to change
- (decision) reconciliation reads identity only, never content — AD-9 holds unchanged; the distinction it never had to draw is identity vs content
- (decision) reconciliation is triggered by a failed existence check, not by every reload — this answers §13 Q12 by mechanism rather than measurement (zero cost in the normal case)
- (decision) a positively identified file is written to the record; absence never is — preserves the existing rule that a transient filesystem condition must not become a permanent claim
- (decision) once the user renames a Note the app stops renaming it (FR-80), inverting AD-18's authority for that case; renaming a user-named file back destroys a deliberate decision
- (decision) deletion moves to the Trash (FR-40 amended) — this is the only gesture in the product that destroys unregenerable audio, and it already cost the user a meeting
- (decision) a Note changed outside Minutes is detected via a stored fingerprint and the user chooses (FR-81); merging stays out of scope permanently
- (assumption) started_at is a sufficient identity key for pre-FR-77 Notes: measured on 15 files, all stamped to the second, no two Meetings within a second — logged as §13 Q22
- (override) the deferral 'Notes Folder conflict policy for hand-edited Notes — out of scope for v1' is partially reversed: detection is in scope (FR-81), merging is not
- (decision) adopting the file's name as the Meeting title is deferred with its reason: it would read content back into state, and a filename is a date prefix plus a slug, not a title
- (decision) watching the Notes Folder with FSEvents is ranked last (§13 Q23) — FR-78 owns the correctness, the watcher owns only the latency
- (event) PRD updated for increment 7; §13 Q12 answered and closed
- (change) increment 8: added §4.13 (FR-84..FR-88) — a recording the app cannot vouch for; amended FR-67 and §9.3
- (constraint) MEASURED: 7 of 16 recordings had a system stream 2x or 3x too fast; all had a Bluetooth input device; every mic stream was correct; transcription did not fail, it fabricated
- (decision) FR-67 amended rather than replaced: signal is necessary and not sufficient. 'Elapsed time is never sufficient' stands — there duration was offered as evidence of capture, in FR-84 it is the denominator of a rate
- (decision) §9.3 gains a floor beneath its labelling rules: every existing honesty rule was satisfied while the product produced invented dialogue, because labelling cannot fix a bad input
- (decision) FR-86 keeps the transcript and refuses only the derived content — the samples are the user's, and discarding them would be worse than labelling them
- (decision) trust is per stream (FR-85), because in every observed case the microphone was correct and the system stream was not; a per-Meeting flag would be wrong in both directions
- (decision) FR-88 makes repair a product capability rather than a one-off: the seven were repaired by hand, and a colleague hitting this needs the same route
- (assumption) the wording in FR-87 names a Bluetooth input device as the likely cause without asserting it — the correlation is 7 of 7 but the mechanism is not proven against Apple's source
- (event) PRD updated for increment 8
- (change) Increment 9: added FR-89..FR-96 (echo detection/exclusion/floor/disclosure, a measurable-accuracy harness, audio-clock rate verification, confidence and gaps); amended FR-6, FR-16, FR-17, FR-21, FR-22, FR-23 and the Glossary (Stream, new term Echo). Driven by measurement: echo affects 3 of 12 dual-stream recordings, 57.6% of mic words duplicated on the worst, six phantom in-room speakers, user never identified; validated fix removes 91% of duplication at a cost of 10.7% of unique mic words.
- (decision) Echo is detected by correlation against the delay-aligned System Stream and excluded, never cancelled: coherence puts the upper bound on ANY linear canceller at 8.7-10.6 dB median against the 20-40 dB a useful AEC needs.
- (decision) Exclusion runs before transcription AND before diarization, because the phantom-attendee failure is a clustering failure that post-hoc transcript de-duplication cannot repair.
- (decision) Mic audio recorded while the System Stream is silent is never excluded (40% of mic activity on the worst recording); concurrent-but-uncorrelated speech is kept as double-talk.
- (decision) An accuracy claim requires a measurement. FR-17's five-point accuracy rating is removed where nothing measured backs it rather than re-estimated.
- (override) My own single-session conclusion that parakeet-v2-en is worse than v3 was RETRACTED: pooled over 3 sessions v2-en leads by 2.3 points on close mics. Per-session swing is +-8 points, larger than the effect.
- (decision) Default model NOT changed despite v2-en leading pooled: three sessions cannot move a default every user gets. Revisit at nine or more sessions spanning native and non-native English (Q19).
- (decision) Canary-1B-v2 rejected on measurement: 8.5 points worse close-mic, 9.2 worse far-field, 22x slower, and returns no timings.
- (decision) Far-field level normalisation deferred: real at ~2 points but needs a gate, and integrated loudness does not separate the conditions (Q20).
- (decision) Long-file chunking rejected as unnecessary: the longest real recording is 57.3 min with 100% transcript coverage.
- (assumption) The echo threshold (0.25 correlation, 0.3 frame-share) was hand-set on three recordings and costs 10.7% of unique mic words on the worst. Needs an energy-dominance formulation and held-out validation.
- (event) PRD increment 9 sections applied
- (override) Story 16.5 REFUTED by measurement: muting Echo before clustering made the in-room voice count WORSE (5->7, 6->6, 3->5) because muting fragments continuous speech and the clusterer splits one voice. Removing audio to fix a speaker count is now forbidden; FR-100 replaces it with far-end embedding exclusion.
- (override) AD-48's blanket prohibition on cancellation was too broad. The measurement (8.7-10.6 dB linear bound) supports forbidding POST-HOC LINEAR cancellation only. The field's answer is linear + non-linear residual + double-talk detection, at capture, which is FR-99.
- (decision) Apple's VoiceProcessingIO is ruled out on structure, not preference: its echo reference is the app's own output bus, so it cannot cancel audio played by the conferencing app. Recorded so it is not rediscovered.
- (decision) Echo handling is gated on the OUTPUT DEVICE (FR-98) rather than on a correlation threshold. Every clean recording was on headphones and every affected one was not, so the fact is available for free where FR-89 spends a search to estimate it.
- (decision) VAD segmentation rejected for this product: measured in the literature to have no significant effect on DELETION rates, and deletions are 290 of our 405 baseline errors. It fixes insertions and hallucination, which increment 8 already addressed.
- (decision) Guided source separation rejected: an array technique. Two channels give a fraction of seven-channel benefit (50.4% / 21.8% relative WER reduction going 2->7 channels), and ours are not even a coherent array.
- (constraint) CHiME-8 DASR: the winning pipeline is diarisation -> separation -> ASR, and diarisation gains exceed ASR gains. Our pipeline is the reverse order. Reordering is noted as a candidate, not adopted on one paper summary.
- (assumption) FR-99's non-linear residual stage is expected to clear the bar the linear stage could not, but that is UNMEASURED on our recordings. FR-90's post-hoc rule stays until the harness says otherwise.
- (change) Increment 10: PRD updated for the build of FR-94..FR-100. Added FR-101 (the pooled figure comes out of the harness); amended FR-94 (rate and continuity are two measurements, tolerance derived per measurement rather than declared) and FR-97 (the start offset is answered at capture, from host times, for every Session and not only affected ones).
- (decision) The device's own sample-time and host-time counters are the time base for BOTH the rate check (FR-94) and the two Streams' start offset (FR-97). One fact, discarded at the callback boundary in both capture adapters, answers two requirements that had been reasoning about it indirectly.
- (override) A published figure did not survive its own rule: 82% proper-noun recall on close mics is the MEAN of 90/78/77. Pooled per FR-93 it is 81%. Corrected rather than quietly restated, and FR-101 adds the command that makes the pooled figure the only one available.
- (assumption) FR-98's device fact is three-valued, not two. Built-in speakers and the headphone jack are separable; Bluetooth is not (AirPods and a Bluetooth loudspeaker report the same transport). Unknown leaves FR-89's detector in charge rather than guessing. Logged as Q23.
- (assumption) FR-100 assumes a far-end voice heard through a loudspeaker still embeds within AD-31's 0.35 of its electrical copy. Calibrated on same-channel voices only. Measured in increment 10 before the story is claimed, because assuming the direction is what went wrong last time. Logged as Q24.
- (decision) Q22 recorded rather than fixed: eight of twenty-one recordings cannot be Echo-checked because FR-88's repair rewrote the header and the streams' rates now differ. The refusal is correct; whether to resample for comparison is a decision, not a defect.
- (event) PRD increment 10 sections applied
- (change) increment 11: added §4.15 (FR-102..FR-105) — capture the app can account for. Driven by measurement: a 42.4-min recording shows 8.37 s of System Stream received and never written (0.33%) while the Mic Stream loses 0.005%, and the 9.17 s file-length difference decomposes into 0.84 s of start/stop offset plus 8.37 s of loss. Two faults, previously read as one.
- (change) increment 11: FR-6 amended — the ±100 ms claim is out by 10x (+1006 ms measured on a real meeting) and has been since increment 1. The claim is retained beside its refutation rather than deleted; FR-105 decides whether it becomes true or becomes honest.
- (decision) Tier 7 ordered instrument-before-fix: FR-102/103 (the accounting identity) must report before any drift fix is chosen, because three mechanisms fit the evidence and reading the code cannot separate them. Refuted already, by measurement: AVAudioConverter per-call tail loss is a constant ~11 frames total over 1500-20000 calls, not per-call — 0.0003%, four orders too small for 0.33%.
- (assumption) Headless: no human confirmed the FR-102..FR-105 split or the Tier 7 ordering. Q25 and Q26 are recorded as open rather than answered.
- (event) PRD updated for increment 11; status stays final, updated 2026-09-07
