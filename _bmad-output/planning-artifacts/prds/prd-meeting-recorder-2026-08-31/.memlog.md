---
topic: Minutes — local-first macOS meeting recorder PRD
goal: Implementable PRD driving epics/stories for a same-day build
updated: 2026-09-01T09:48
---

- (event by coach) Headless create. Input: brief.md + addendum.md from brief-meeting-recorder-2026-08-31. No interactive discovery possible; user is away until end of day.
- (decision by coach) Stakes read as personal/internal utility, not launch. PRD targets ~5-8 pages: lean on business sections, heavy on FRs because these feed epics/stories and a same-day build directly.
- (decision by coach) Adapt-In clusters pulled: Cross-Cutting NFRs, Constraints and Guardrails (privacy/cost), Performance Budgets, Aesthetic and Tone (brief demands a 'small clean UI'). Invented section 'Platform, Permissions and Signing' — TCC consent keyed to code signature with no Developer ID present is the product's single largest risk and no template cluster names it.
- (decision by coach) Dropped Monetization, ROI, Stakeholders/Approvals, Rollout, Compliance/Regulatory, Integration and Dependencies: single-user local tool with no business model, no external systems and no regulator.
- (decision by user) MID-RUN USER DIRECTIVE (with FluidVoice screenshot): 'I like the way fluid voice sets up the models. Do something similar.' FluidVoice pattern to adopt: single window with grouped sidebar (Configure/Use/Activity/Help); a 'Quick Setup' checklist card whose rows carry state icon + title + one-line explanation + right-aligned control (dim 'Done' pill when satisfied, actionable button when not); completed rows de-emphasised; 'Run Onboarding Again' affordance; and a 'Test Playground' card that proves the pipeline end-to-end in-app.
- (decision by coach) Adopting the Test Playground is the highest-value part of the directive: macOS exposes no API to query system-audio-capture permission (see FR-42), so an in-app record-and-transcribe test converts an unanswerable question into an empirical one. Promoted from nice-to-have to a requirement.
- (change by coach) IA consolidated as a consequence: replaced the planned separate Settings window + Library window with ONE sidebar window (Setup / Transcription / Meetings / Detection / General). Net surface area roughly unchanged, so SM-C2 is respected; two windows collapsing into one is a simplification, not an addition.
- (change by coach) Added FR-46 (Setup Checklist), FR-47 (Test Playground), FR-48 (re-run onboarding), and section 'Information Architecture and Visual Design' capturing the sidebar shape and the checklist row anatomy.
- (event by coach) Reviewer gate run: rubric walker across all 7 dimensions -> review-rubric.md. Verdict: implementable and well-grounded; 2 high findings (assumption density inappropriate for a green-light PRD; 48 FRs vs the user's word 'small'). Fixed in place: added §6.3 Build Order tiers (all 48 FRs assigned, verified programmatically), stated the thesis in one sentence in §1, marked SM-1 provisional pending measurement, plain-worded FR-20, folded §9.3. Assumption density cannot be fixed without the user — escalated in addendum §5.
- (event by coach) PRD finalized
- (change) FR-49/FR-50 added: elapsed time on the menu bar label and an animated Recording indicator. FR-4 already required elapsed time inside the open menu; the user wanted it visible without opening the menu, which is a different requirement.
- (change) FR-51 added: remembered voices are listable, renamable and individually forgettable. FR-25 created Speaker Profiles but gave the user no way to see or curate them, so a wrong match was only correctable via a meeting that happened to contain that voice.
- (change) FR-52 added: the Metadata Backend is named and selectable in Settings. FR-30 recorded provenance in the Note only, so a user reading the app could not tell what produced a summary, or that Apple Intelligence being off was the reason.
- (change) FR-53 added: Note presence is verified against the Notes Folder and a missing Note is rewritable. Observed defect: 7 Meetings recorded stage=written with a noteFilename while only 2 files existed on disk, and nothing reconciled the record against the folder.
- (change) FR-54 added and FR-40 amended: the Library refreshes from disk, and Delete is reachable without discovering a context menu. The user asked to manage meetings in-app and did not find the existing context-menu delete.
- (decision) Note-to-record reconciliation is read-only about the Note: a missing Note is reported and offered for rewrite from meeting.json, never inferred back into the record. Keeps AD-9 (the Note is a projection, never parsed back) intact.
- (decision) The Recording animation is confined to the Recording state. FR-5's feature NFR already forbade animating while Idle; that constraint is now explicit rather than implied.
- (assumption) A per-voice rename in the voices list renames the Profile only, not the Speaker Labels in already-written Meetings. Retro-editing history was not requested and would rewrite Notes the user may have edited.
- (override) FR-27 amended: 'prefer the LLM Backend' is now a default the user may override via FR-52. Reverses the implied rule that availability alone decides.
- (override) 6.2 corrected: 'Recovery UI for crash-orphaned Sessions' was listed out of scope for MVP but has been built. The original rationale understated the symptom - an interrupted Session span a spinner forever and read as a hang. Covered by FR-39's surface; no new FR.
- (event) Reviewer pass (inline, hobby-stakes): 54 FRs, every one tiered in 6.3, verified programmatically. Two conflicts with prior decisions found and reconciled rather than deferred. 4 new open questions raised, all measurement-shaped and none blocking.
- (event) bmad-review run on the proposal 'gate summaries on a real LLM; user supplies an API key'. Lenses: adversarial (14 findings) + edge-case-hunter (13 unhandled paths). Verdict: split the proposal. Report at review-llm-key-proposal.md/.html.
- (decision) Decision A (gate the summary on a real model) recommended for acceptance and can ship independently: omit the summary/decisions/actions sections entirely rather than filling them with sentence extracts, per 9.3. Requires an FR-26 amendment so the title guarantee survives while the summary becomes conditional.
- (decision) Decision B (where the intelligence comes from) NOT taken pending evidence. Blocking finding: FoundationModelsBackend is already built and the M5/24GB machine is eligible - availability reports appleIntelligenceNotEnabled and opted_out_buddy=1, i.e. Apple Intelligence was declined at Setup Assistant. Enabling it costs nothing and breaks no NFR. Cloud key is option 3 of 3 and the only one that violates NFR-1/NFR-6/9.2.
- (assumption) Reviewer assumption: the weak summaries are partly a transcription problem, not only a summarisation one - 'preaching page' is a Whisper error on the base model. Recommend re-transcribing one meeting with large-v3-turbo before any Decision B work, since a wrong transcript makes every option produce a fluent wrong summary.
- (direction) User decision: do NOT rely on Apple Intelligence, but keep it as one option. Add a summarisation-intelligence settings pane mirroring the transcription model picker, offering a local downloadable LLM or a user-supplied key. Run the full BMAD flow, stop before implementation.
- (event) Spike run before requirements: MLX local LLM is feasible (mlx-swift 0.31.6 / mlx-swift-examples 2.29.1 compile and link, HubApi downloads into our own models dir with real progress). Hard blocker found: Xcode Metal Toolchain component is uninstalled, so no metallib is built and MLX cannot run a single token. Report at planning-artifacts/spikes/spike-local-llm-2026-08-31.md
- (change) Increment 3 PRD update applied: FR-55 to FR-61 added, section 4.10 Summarisation Intelligence created, FR-26/FR-27/FR-52 amended, glossary Metadata Backend expanded from 2 to 4 members plus Summarisation Backend and Prerequisite, Tier 5 build order added, 5 open questions and 5 assumptions recorded. 61 FRs, all tiered.
- (override) NFR-1 amended for the first time in the project: 'no audio, Transcript or Metadata transmitted anywhere, ever' becomes 'on-device by default; egress only by explicit per-use consent'. Audio and Speaker Profiles stay absolute never-transmit invariants. Done in the open because FR-59 makes a Transcript transmissible - the alternative was shipping code that contradicts a standing NFR.
- (override) NFR-6 amended: the egress claim is now conditional, so it is stated more precisely - zero traffic with no Remote Backend configured, traffic only to the configured endpoint when a Meeting was sent, never audio.
- (override) 9.2 zero-cost renegotiated rather than broken: it remains the constraint on every path shipped enabled; the Remote Backend is off by default, must never become the default, and FR-60 requires cost shown before it is incurred.
- (decision) FR-27 demoted: Apple's model is one entry in a list, not a privileged tier, on the user's explicit instruction not to rely on it while keeping it available. The demotion is itself the requirement.
- (decision) Tier 5 ordered so FR-55 (no summary unless something real produced it) ships first and standalone, and FR-59 (remote key) ships last - it is the only transmitting part and carries an unresolved consent finding.
- (change) Story 9.1 built: HeuristicBackend now returns title and tags only, no summary/decisions/actionItems. Note is frontmatter + Transcript with speaker IDs. Extraction functions retained unused and still unit-tested - re-enabling is a one-line change, which matters because action-item extraction was the best of the three.
- (change) Audio storage changed to 16 kHz mono 16-bit via AVAudioConverter in StreamFileWriter. Measured 532 KB/s -> 64 KB/s, 8.4x smaller; a 45-min meeting goes 1.47 GB -> 176 MB. Nothing consumed the old resolution: both engines and the diarizer resample to 16 kHz mono on read. Added a performance budget so a future regression has something to violate.
- (assumption) Tags kept in the note frontmatter under story 9.1. The user said 'titles and pure transcripts thats it', which most directly names the visible prose sections; tags are frontmatter metadata for filtering and story 9.1 as approved retains them. Trivially removable if that reading is wrong.
- (change) First real meeting (2026-09-01, 8-person Slack huddle) exposed three defects. Detection worked but the notification hid Record behind 'Options', so the panel is now the primary prompt and the notification a durable record. Debounce 4.0->2.5s, poll 2.0->1.0s.
- (change) Auto-stop never fired because the session was started manually and triggeringApp was nil - FR-14 restricted auto-stop to prompt-started sessions, which had been implemented as 'do nothing at all'. A manual start now adopts whichever watched app holds the input, and a manual session gets a stop PROMPT (StopPanel) while a detected one still auto-stops.
- (change) Mic mute added (menu bar, while recording). Muting in Slack stops Slack's outgoing stream, not the macOS input device, so Minutes kept recording the room. Silence is written rather than frames dropped, so both streams stay on one clock (AD-4).
- (assumption) Measured on the real meeting: 723 of 1501 transcript words (48%) were the conversation beside the user, not the meeting. Minutes 4-8 contain zero far-end speech and only in-room speech, which most likely means the huddle ended around minute 4 and recording continued on the room - tying the missing stop prompt directly to the polluted transcript. Needs formal PRD capture as an increment-4 requirement set.
